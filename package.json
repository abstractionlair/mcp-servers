{
  "name": "@abstractionlair/mcp-server-codex-review",
  "version": "1.0.0",
  "description": "MCP server for requesting Codex (GPT-5) methodology reviews",
  "type": "module",
  "bin": {
    "mcp-server-codex-review": "./build/index.js"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "watch": "tsc --watch"
  },
  "keywords": ["mcp", "codex", "gpt-5", "claude-code"],
  "author": "abstractionlair",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "files": [
    "build/**/*"
  ]
}
